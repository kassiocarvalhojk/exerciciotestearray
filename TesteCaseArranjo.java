package arranjoExercicio;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class TesteCaseArranjo {

	@Test
	void menoTest() {
		
		Arranjo a = new Arranjo();
		
		// Testa se o menor é 510
		
		assertEquals(510, a.menor(), "O menor deve ser 440");
		
		// Testa se o maior é 940
		
		//assertEquals(940, a.maior(), "O maior deve ser 940");
		
		// Testa se a soma é 7540
		
		//assertEquals(7540, a.soma(), "A soma é 940");
		
		// Testa a quantidade de vezes que o nuúmero  3 aparece
				
		//assertEquals(0, a.repeticoes(3), "940 aparece 2 vezes");
		
		// Testa a quantidade de vezes que o nuúmero  3 aparece
		
		//assertEquals(1, a.repeticoes(790), "940 aparece 2 vezes");
		
		// Testa a quantidade de vezes que o nuúmero  3 aparece
		
		//assertEquals(2, a.repeticoes(940), "940 aparece 2 vezes");
						
				
				
				
	}

}